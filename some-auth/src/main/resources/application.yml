server:
  servlet:
    #    context-path: /auth
    encoding:
      charset: UTF-8
      enabled: true

spring:
  profiles.active: local

  # Redis
  redis:
    host: localhost
#    host: 3.35.149.202
    port: 6379

  # DataSource
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://3.35.18.146:3306/some?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false&allowPublicKeyRetrieval=true
    username: some
    password: '!23Qwe'
    hikari:
      pool-name: jpa-hikari-pool
      maximum-pool-size: 5
      jdbc-url: ${spring.datasource.url}
      username: ${spring.datasource.username}
      password: ${spring.datasource.password}
      driver-class-name: ${spring.datasource.driver-class-name}
      data-source-properties:
        rewriteBatchedStatements: true

  # JPA
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        hbm2ddl.import_files_sql_extractor: org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor
        current_session_context_class: org.springframework.orm.hibernate5.SpringSessionContext
        default_batch_fetch_size: ${chunkSize:100}
        jdbc.batch_size: 20
        order_inserts: true
        order_updates: true
        format_sql: true

  # Security OAuth
  security:
    oauth2.client:
      registration:
        kakao:
          clientId: ed5919bad756edb432bdb8d297df428a
          clientSecret: W6tE0Y3CE0mcVq8crwyGIgtCZdFD5m6Q
          clientAuthenticationMethod: post
          authorizationGrantType: authorization_code
          redirectUri: "http://localhost:8080/login/oauth2/code/kakao"
          scope:
            - profile_nickname
            - profile_image
            - account_email
          clientName: Kakao

      provider:
        kakao:
          authorizationUri: https://kauth.kakao.com/oauth/authorize
          tokenUri: https://kauth.kakao.com/oauth/token
          userInfoUri: https://kapi.kakao.com/v2/user/me
          userNameAttribute: id

# cors
cors:
  allowed-origins: 'http://localhost:3000'
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: '*'
  max-age: 3600

# jwt secret key
jwt.secret: '8sknjlO3NPTBqo319DHLNqsQAfRJEdKsETOds'

# jwt 토큰 관련 secret Key 및 RedirectUri
app:
  auth:
    tokenSecret: 926D96C90030DD58429D2751AC1BDBBC
    tokenExpiry: 1800000
    refreshTokenExpiry: 604800000
  oauth2:
    authorizedRedirectUris:
      - http://localhost:3000/oauth/redirect

# S3
cloud:
  aws:
    s3:
      bucket: k8b301-bucket
    credentials:
      access-key: AKIAY7TIHYUIPS3AMUHV
      secret-key: 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
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false